{
  "name": "create-user",
  "version": "1.0.0",
  "description": "creates user object in the database",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && babel src/ -d dist/ --copy-files",
    "clean": "rimraf dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/runtime": "^7.0.0",
    "aws-sdk": "^2.316.0",
    "bcryptjs": "^2.4.3"
  }
}
